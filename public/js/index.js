// let items = ["Qui suis-je ?", "Mes passions", "Mes compétences", "Mes projets"]

$(function(){

    // for (let i=0 ; i < items.length ; i++) {
    //     let button = $('<a/>');
    //     button.append($('<button/>',{text:items[i]}));
    //     $('.div-boutons-defile').append(button);
    // }
    // $('body').data('projetsData',$.parseJSON($('#projetsData').val()));
    // let projetsData = $('body').data('projetsData');
})